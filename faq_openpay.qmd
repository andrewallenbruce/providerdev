---
title: "Open Payments API"
editor_options: 
   chunk_output_type: console
---
```{r}
#| label: setup-common-01
#| include: false
source("includes/_common.R")

# "[^\\x00-\\x7F]"
```

## Dataset

   * `publisher`: `"openpaymentsdata.cms.gov"`
   * `bureauCode`: `"009:38"`
   * `programCode`: `"009:000"`
   * `accessLevel`: `"public"`
   * limit = `500`

```{r}
#| label: open_describedby
open_describedby <- function(x) {
  
  x <- map(
    get_elem(
      get_elem(x$distribution, 
               "data", 
               DF.as.list = TRUE),
      "title|describedBy",
      regex = TRUE), 
    function(x) x[not_null(names(x))]
    )
  
  new_df(
    title       = delist(get_elem(x, "title")),
    describedBy = delist(get_elem(x, "describedBy"))) |>
    mtt(
      year = as_int(stri_extract_all_regex(title, "[0-9]{4}")),
      name = cheapr_if_else(na(year), title,
                            stri_extract_all_regex(
                              title, 
                              "^.*(?=\\s.\\sDetailed Dataset [0-9]{4} Reporting Year)")),
      year = cheapr_if_else(na(year), fmax(year), year)) |>
    sbt(year == fmax(year), 
        -year, 
        -title)
}
```


```{r}
#| label: open_downloadurl
open_downloadurl <- function(x) {
  
  add_vars(x, 
           downloadURL = delist(
             get_elem(
               get_elem(x$distribution, 
                        "data", 
                        DF.as.list = TRUE),
               "downloadURL"
               )
             )
           )
}
```


```{r}
#| label: load_open
load_open <- \() {
  
  dataset <- as_tbl(
  mtt(
    fload("https://openpaymentsdata.cms.gov/api/1/metastore/schemas/dataset/items?show-reference-ids"), 
    modified    = as_date(modified), 
    description = replace_fixed(description, c("\n", "\r. \r.", '"'), c(". ", "", "")),
    theme       = delist(map(theme, function(x) get_elem(as.list(x), "data"))), 
    year        = delist(map(keyword, function(x) get_elem(as.list(x), "data"))), 
    year        = replace_fixed(year, c("all years"), c("All")),
    year        = cheapr_if_else(title == "Provider profile ID mapping table", "All", year),
    identifier  = paste0("https://openpaymentsdata.cms.gov/api/1/datastore/query/", identifier, "/0")))
  
  x <- rsplit(slt(open_downloadurl(dataset),
      year,
      theme,
      title,
      description,
      modified,
      temporal,
      identifier,
      downloadURL), ~ theme)
  
  list(
    summary_all  = roworder(sbt(x$Summary, year == "All", -year), title),
    summary_year = roworder(mtt(sbt(x$Summary, year != "All"), 
                       year = as_int(year),
                       title = stri_replace_all_regex(title, "^[0-9]{4} ", "")), title, -year),
    general      = mtt(x$`General Payments`, year = as_int(year), title = NULL),
    research     = mtt(x$`Research Payments`, year = as_int(year), title = NULL),
    ownership    = mtt(x$`Ownership Payments`, year = as_int(year), title = NULL),
    dictionary   = open_describedby(dataset))
  
}

load_open <- memoise::memoise(load_open)

dataset <- load_open()

dataset
```

## Dictionaries

```{r}
#| label: open_dictionary
open_dictionary <- function(x, nm) {
  
  nm <- arg_match0(nm, c("Covered", "Research", "Ownership", "General"))  
  
  x <- sbt(x$dictionary, stri_detect_regex(name, paste0("^", nm))) |> 
    _[["describedBy"]] |> 
    request() |> 
    req_perform() |> 
    resp_simple_json() |> 
    as_tbl() |> 
    _[["data"]]
  
  roworder(
    new_tbl(
      field = delist(get_elem(x$fields, "name")),
      description = delist(get_elem(x$fields, "description"))), 
    field)
}

open_dictionary <- memoise::memoise(open_dictionary)
```

### Covered Recipient Profile Supplement

```{r}
#| label: dict_covered
dict_covered <- open_dictionary(dataset, "Covered")

dict_field <- cli::style_underline(dict_covered$field)

dict_desc <- cli::ansi_strwrap(dict_covered$description, width = 50, indent = 2, exdent = 2)

cli::cli_text("{dict_covered$field}: {dict_covered$description}\n\n")

cli::cli_bullets(
  c(
    "*" = "{dict_covered$field}",
    " " = "{dict_covered$description}"
  )
)
```

### General Payment File

```{r}
#| label: dict_general
open_dictionary(dataset, "General") |> 
  _[["description"]] |> 
  cat(sep = "\n \n")
```

### Ownership Payment File

```{r}
#| label: dict_ownership
open_dictionary(dataset, "Ownership") |> 
  _[["description"]] |> 
  cat(sep = "\n \n")
```

### Ownership Payment File

```{r}
#| label: dict_research
open_dictionary(dataset, "Research") |> 
  _[["description"]] |> 
  cat(sep = "\n \n")
```
